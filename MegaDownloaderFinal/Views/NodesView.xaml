<UserControl x:Class="MegaDownloaderFinal.Views.NodesView"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0" Text="{Binding NodesFilter, UpdateSourceTrigger=PropertyChanged}" />

        <Border
            Grid.Row="1"
            Margin="0 10 0 0"
            Padding="5"
            BorderBrush="Gray"
            BorderThickness="1"
            CornerRadius="5">
        <telerik:RadTreeListView x:Name="Nodes"
								 IsReadOnly="True"
								 ItemsSource="{Binding NodesCollectionView}"
                                 IsExpandedBinding="{Binding IsExpanded, Mode=TwoWay}"
								 Width="600" 
								 MinHeight="300"
								 Height="550"
								 CanUserFreezeColumns="False"
								 RowIndicatorVisibility="Collapsed"
								 ColumnWidth="*">
            <telerik:RadTreeListView.ChildTableDefinitions>
                <telerik:TreeListViewTableDefinition ItemsSource="{Binding Items}" />
            </telerik:RadTreeListView.ChildTableDefinitions>
        </telerik:RadTreeListView>
        </Border>
    </Grid>


</UserControl>
